<%= form_tag(search_admin_courses_path, :method => "get") do %>
	<div class="input-group">
		<%= search_field_tag :search, params[:search], placeholder: "Type" %>
		<div class="input-group-btn">
			<%= button_tag "Search for Course", :class => 'btn btn-secondary glyphicon glyphicon-search', :name => nil %>
		</div>
	</div>
<% end %>
