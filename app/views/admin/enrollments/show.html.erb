<p>
  <strong> ID : </strong> 
  <%= @enrollment.id %>
</p>

<p>
  <strong> First Name : </strong> 
  <%= @student.first_name %>
</p>

<ul>
  <li><%= link_to "Edit", edit_admin_student_path(@student) %></li>
  <li><%= link_to "Destroy", admin_student_path(@student), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<%= link_to "Back", admin_session_index_path %>
